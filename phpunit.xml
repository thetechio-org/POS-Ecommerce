<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_STORE" value="array"/>
        <!--
            Every Feature test uses RefreshDatabase, which DROPS AND REBUILDS the
            database it is pointed at. With these commented out the suite ran against
            whatever .env specified — so `php artisan test` on a live server would
            have destroyed production data.

            An in-memory SQLite database is not an option here: several migrations
            use raw MySQL DDL (ALTER TABLE ... MODIFY ... ENUM). So the suite is
            pinned to a separate MySQL schema instead. Create it once with:
                CREATE DATABASE pos_testing;
            If it does not exist the suite fails immediately — which is the safe
            way to fail.
        -->
        <env name="DB_DATABASE" value="pos_testing"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
    </php>
</phpunit>
